# TigerSqlCmd 0.8.7 broad-benchmark package (TigerSetup). The common
# contract is ../../contract.md; this file is generated by
# scripts/New-BroadPackages.ps1 from the campaign's corpus record. The
# payload is the canonical TigerSqlCmd payload, referenced relative to this
# manifest.

[package]
id = "Benchmark.TigerSqlCmd"
name = "TigerSqlCmd"
version = "0.8.7"
publisher = "TigerSetup broad benchmark"

[install]
scopes = ["user"]

[[files]]
source = "../../../../compression-spike/corpus/TigerSqlCmd/payload/**"