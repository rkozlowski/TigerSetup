# VSCode 1.138.0 broad-benchmark package (TigerSetup). The common
# contract is ../../contract.md; this file is generated by
# scripts/New-BroadPackages.ps1 from the campaign's corpus record. The
# payload is the canonical VSCode payload, referenced relative to this
# manifest.

[package]
id = "Benchmark.VSCode"
name = "VSCode"
version = "1.138.0"
publisher = "TigerSetup broad benchmark"

[install]
scopes = ["user"]

[[files]]
source = "../../../../compression-spike/corpus/VSCode/payload/**"