# VLC 3.0.23 broad-benchmark package (TigerSetup). The common
# contract is ../../contract.md; this file is generated by
# scripts/New-BroadPackages.ps1 from the campaign's corpus record. The
# payload is the canonical VLC payload, referenced relative to this
# manifest.

[package]
id = "Benchmark.VLC"
name = "VLC"
version = "3.0.23"
publisher = "TigerSetup broad benchmark"

[install]
scopes = ["user"]

[[files]]
source = "../../../../canonical/VLC/payload/**"