; WinSCP 6.5.7 broad-benchmark package (Inno Setup 7). The common
; contract is ..\..\contract.md; this file is generated by
; scripts\New-BroadPackages.ps1 from the campaign's corpus record. The
; payload is the canonical WinSCP payload, referenced relative to this script.
;
; Compression: LZMA2/max, solid -- the benchmark's production-quality
; setting for all three technologies (see benchmark\README.md).

#define MyAppName "WinSCP"
#define MyAppVersion "6.5.7"
#define MyAppPublisher "TigerSetup broad benchmark"
#define PayloadDir "..\..\..\..\compression-spike\corpus\WinSCP\payload"

[Setup]
AppId=Benchmark.WinSCP
AppName={#MyAppName}
AppVersion={#MyAppVersion}
AppPublisher={#MyAppPublisher}
DefaultDirName={autopf}\{#MyAppName}
DisableProgramGroupPage=yes
PrivilegesRequired=lowest
ArchitecturesAllowed=x64compatible
ArchitecturesInstallIn64BitMode=x64compatible
Compression=lzma2/max
SolidCompression=yes
OutputDir=..\..\..\..\artifacts\broad\winscp
OutputBaseFilename=WinSCP-InnoSetup

[Files]
Source: "{#PayloadDir}\*"; DestDir: "{app}"; Flags: recursesubdirs ignoreversion