; NotepadPlusPlus 8.9.8 broad-benchmark package (NSIS 3.12). The common
; contract is ..\..\contract.md; this file is generated by
; scripts\New-BroadPackages.ps1 from the campaign's corpus record. The
; payload is the canonical NotepadPlusPlus payload, referenced relative to this script.
;
; Compression: LZMA, solid -- the benchmark's production-quality setting for
; all three technologies (see benchmark\README.md).

Unicode true

!define APP_NAME "NotepadPlusPlus"
!define APP_VERSION "8.9.8"
!define APP_PUBLISHER "TigerSetup broad benchmark"
!define PAYLOAD_DIR "..\..\..\..\compression-spike\corpus\NotepadPlusPlus\payload"
!define UNINSTALL_KEY "Software\Microsoft\Windows\CurrentVersion\Uninstall\Benchmark.NotepadPlusPlus"

!include "FileFunc.nsh"

Name "${APP_NAME}"
; The output path is the definition's own unless the build passes
; /DOUTFILE=<path> (Build-Installers.ps1 does, per campaign).
!ifndef OUTFILE
  !define OUTFILE "..\..\..\..\artifacts\broad\notepadplusplus\NotepadPlusPlus-NSIS.exe"
!endif
OutFile "${OUTFILE}"
InstallDir "$LOCALAPPDATA\Programs\${APP_NAME}"
RequestExecutionLevel user
SetCompressor /SOLID lzma

VIProductVersion "${APP_VERSION}.0"
VIAddVersionKey "ProductName" "${APP_NAME}"
VIAddVersionKey "ProductVersion" "${APP_VERSION}"
VIAddVersionKey "CompanyName" "${APP_PUBLISHER}"
VIAddVersionKey "FileVersion" "${APP_VERSION}.0"

Page directory
Page instfiles
UninstPage uninstConfirm
UninstPage instfiles

Section "Install"
  SetShellVarContext current
  SetOutPath "$INSTDIR"
  File /r "${PAYLOAD_DIR}\*.*"
  WriteUninstaller "$INSTDIR\Uninstall.exe"

  WriteRegStr HKCU "${UNINSTALL_KEY}" "DisplayName" "${APP_NAME}"
  WriteRegStr HKCU "${UNINSTALL_KEY}" "DisplayVersion" "${APP_VERSION}"
  WriteRegStr HKCU "${UNINSTALL_KEY}" "Publisher" "${APP_PUBLISHER}"
  WriteRegStr HKCU "${UNINSTALL_KEY}" "InstallLocation" "$INSTDIR"
  WriteRegStr HKCU "${UNINSTALL_KEY}" "UninstallString" '"$INSTDIR\Uninstall.exe"'
  WriteRegStr HKCU "${UNINSTALL_KEY}" "QuietUninstallString" '"$INSTDIR\Uninstall.exe" /S'
  ${GetSize} "$INSTDIR" "/S=0K" $0 $1 $2
  WriteRegDWORD HKCU "${UNINSTALL_KEY}" "EstimatedSize" "$0"
  WriteRegDWORD HKCU "${UNINSTALL_KEY}" "NoModify" 1
  WriteRegDWORD HKCU "${UNINSTALL_KEY}" "NoRepair" 1
SectionEnd

Section "Uninstall"
  SetShellVarContext current
  RMDir /r "$INSTDIR"
  DeleteRegKey HKCU "${UNINSTALL_KEY}"
SectionEnd